# Server id
server_id = 90564452600745770

# Role assigned by default to every member that joins.
# Set it to `None` to not assign any role.
default_role = 'Test'

# Roles that can be assigned with the `role` command.
assignable_roles = ['Test']

welcome_channel_id = '801482154680647742' # 'bienvenida-y-reglas'


role_codes = {
    'secret_code': 749046557054301980 # This number is the role id.
}